/**
 * Central export file for all custom React hooks.
 *
 * Provides reusable stateful logic that can be shared across components.
 */

export * from './useAuth' // Authentication management hook
export * from './useTheme' // Theme management hook for dark/light mode switching
