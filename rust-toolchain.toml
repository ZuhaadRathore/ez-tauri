# Rust toolchain configuration for consistent builds across environments
[toolchain]
channel = "stable"                    # Latest stable Rust version for reproducible builds
components = ["rustfmt", "clippy"]    # Code formatting and linting tools
profile = "minimal"                   # Minimal profile for faster installs